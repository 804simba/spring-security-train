server:
  port: 9000

spring:
  datasource:
    password: ''
    url: jdbc:postgresql://localhost:5432/userRegistration
    username: ''
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
